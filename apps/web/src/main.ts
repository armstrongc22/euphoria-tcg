/**
 * Placeholder for the public card viewer website. The framework choice
 * (likely Vite + a UI library) is deferred; this stub keeps the workspace
 * wired up and typechecking until then.
 */
import type { Card } from "@euphoria/card-data";

export type CardViewerProps = {
  cards: Card[];
};
